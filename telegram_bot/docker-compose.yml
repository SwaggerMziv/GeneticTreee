services:
  telegram_bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: genetic_tree_bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
